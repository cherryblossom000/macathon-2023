{
	"name": "backend",
	"version": "1.0.0",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"build": "pnpm -C .. run copy-data && tsc -b",
		"watch": "tsc -b -w",
		"lint": "eslint --ext cjs,ts .",
		"deploy": "pnpm -C .. run deploy",
		"test": "tsc -b tsconfig.test.json && node dist/test.js"
	},
	"dependencies": {
		"fp-ts": "^2.16.1",
		"io-ts": "^2.2.20",
		"shared": "workspace:^1.0.0"
	},
	"devDependencies": {
		"@types/node": "^20.5.9",
		"@vercel/node": "^3.0.4",
		"typescript": "~5.2.2"
	}
}
